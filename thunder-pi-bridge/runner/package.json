{
	"name": "thunder-pi-bridge",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Thunder LLM transport sidecar backed by @earendil-works/pi-ai",
	"scripts": {
		"build": "npx esbuild bridge.src.mjs --bundle --platform=node --format=esm --outfile=bridge.mjs"
	},
	"dependencies": {
		"@earendil-works/pi-ai": "0.87.1"
	}
}
